FROM nginx:alpine

# Copy a simple HTML page
COPY simple-admin.html /usr/share/nginx/html/index.html

# Copy nginx configuration
COPY nginx-simple.conf /etc/nginx/conf.d/default.conf

# Expose port
EXPOSE 3000

# Start nginx
CMD ["nginx", "-g", "daemon off;"]
